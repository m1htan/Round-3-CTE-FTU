Round-3-CTE-FTU/
  config/
    settings.yml                 # thông số scale, spam, cooldown, SLO,...
    .env
  data/
    eod/                         # cache EOD
    intraday_15m/                # cache intraday 15m
    backfill/                    # snapshot nến & tín hiệu để replay
  logs/
    realtime/2025-09-13.csv      # mọi hoạt động ghi CSV
  main/
    adapters/
      fiin_session.py            # login, retry, rate-limit
      fiin_intraday.py           # lấy OHLCV 15′ theo sàn/mã
      fiin_eod.py                # fundamentals + EOD
    features/
      tech_15m.py                # RSI, MACD, ADX, BB, ATR, ST, PSAR (15′)
      merge_eod_filter.py        # lọc 40% valuation + EPS YoY > 0 + ROE > 0
    engine/
      signal_rules.py            # BUY/SELL/RISK + early + debounce(1 nến)
      state_store.py             # in_position, last_signal, cooldown, alert_sent_at
    alerts/
      email_sender.py            # SMTP + template + throttle
    app/
      streamlit_app.py           # realtime + hiệu suất 30/60
    orchestration/
      scheduler.py               # poll 15′, EOD job; SLO guard
      replay.py                  # backfill & kiểm thử
  tests/
    test_indicators_15m.py
    test_signal_rules.py
    test_email_sender.py
    test_scheduler_replay.py
  README.md
